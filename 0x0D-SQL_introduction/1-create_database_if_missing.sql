-- SQL script to create the database hbtn_0c_0 if it doesn't exist

-- Connect to the MySQL server and create the database if it doesn't exist
/* This script creates the database hbtn_0c_0 if it doesn't exist already */

CREATE DATABASE IF NOT EXISTS hbtn_0c_0;
